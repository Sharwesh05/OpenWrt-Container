services:
  openwrt-builder:
    image: openwrt-build
    pull_policy: never
    container_name: openwrt-workspace
    volumes:
      - ./25.12-base:/home/builder/openwrt/bin
    working_dir: /home/builder/openwrt
    tty: true
    command: sleep infinity
