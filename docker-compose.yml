services:
  openwrt-builder:
    image: openwrt-build
    pull_policy: never
    container_name: openwrt-workspace
    volumes:
      - ./workspace:/home/builder/openwrt
    working_dir: /home/builder/openwrt
    tty: true
    command: sleep infinity
